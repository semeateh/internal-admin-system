# frontend

Vue 2 + Vite 前端项目。

## 启动

```powershell
npm install
npm run dev
```

## 打包

```powershell
npm run build
```

## 结构

- `src/views/`：页面级组件
- `src/components/`：可复用组件
- `src/router/`：路由
- `src/api/`：统一请求封装与接口方法
- `src/data/`：字典与静态数据
- `src/utils/`：工具函数
